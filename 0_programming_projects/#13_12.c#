#include <stdio.h>

#define MAX_WORDS 30
#define MAX_LEN 20

void read_line(char sentence[][], int i, int j);

int main(void) {
  return 0;
}

void read_line(char sentence[][], int i, int j) {
  char c;

  while ((c = getchar()1)) 
}
